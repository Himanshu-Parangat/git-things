name: Dependency resolution 

on:
  workflow_call:

jobs:
  dependency_resolve:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: 'stable'

      - name: make file finding
        run: echo "finding make file"

      - name: Listing all file
        run: ls --all

      - name: Fetch Dependency
        run: make
